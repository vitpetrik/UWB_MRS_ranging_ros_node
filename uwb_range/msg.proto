// A very simple protocol definition, consisting of only
// one message.

syntax = "proto3";

message beacon_msg {
    uint32 uav_type = 1;
    repeated double GPS = 2;
}

message ranging_response_msg {
    uint32 delay = 1;
}